<div class="row">
  <div class="col-sm-10 col-sm-offset-1">
    <%= simple_form_for(@user) do |f| %>
      <%= f.error_notification %>
          <%= f.association :courses, as: :check_boxes, label: "Cochez les cours qui vous intéressent" %>
          <%= f.association :timeslots, as: :check_boxes, label: "Tous les cours sont disponibles aux horaires suivants :" %>
        <%= f.submit class: "inscription", value: "Envoyer"%>
    <% end %>
  </div>
</div>
